interface:
  display_name: "C# LSP"
  short_description: "Query C# projects through LSP sessions"
  default_prompt: "Use $csharp-lsp to inspect C# definitions, references, hover text, symbols, diagnostics, and reuse the correct LSP daemon session."
